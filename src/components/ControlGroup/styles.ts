import { Vexile } from '@haechi/flexile'
import { styled } from 'incart-fe-common'

export default {
    Wrapper: styled(Vexile, {
        lightBorder: {
            color: '$grey1',
        },
        borderRadius: '3rem',
        clickAnimation: true,
    }),
}
